<% if @edit %>
  <% for position in 0..3 %>
      
  
  <%= 
    select_tag "periods[#{period.id}]position[#{position}]", "<option></option>" + 
      options_from_collection_for_select(@all_talks, 'id', 'option_text', period.talk_list[position] && period.talk_list[position].id)  
  %>
    
  <% end %>
<% else %>
  <%= render :partial => period.talks %>
<% end %>