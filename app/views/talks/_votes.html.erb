<% if !current_user %>
  <%= link_to "Logg inn for å stemme på lyntalen", new_user_session_url %>
<% elsif @talk.votes.find(:all, :conditions => { :user_id => current_user }).empty? %>
  <%= link_to "Stem på denne lyntalen", talk_votes_url(@talk), :method => :post %>
<% else %>
  <em>Du har stemt på denne lyntalen</em>
<% end %>
