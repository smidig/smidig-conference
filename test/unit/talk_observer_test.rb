# -*- encoding : utf-8 -*-
require 'test_helper'

class TalkObserverTest < ActiveSupport::TestCase

  test "should twitter only in production, so kinda hard to test without pointless mocking!" do
    assert true
  end
end
